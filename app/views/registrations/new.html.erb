<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register - VR Tourism</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: url('<%= asset_path('549551.jpg') %>') no-repeat center center fixed;
            background-size: cover;
            margin: 0;
            padding: 0;
            height: 100vh;
        }

        .register-container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .register-form {
            position: relative;
            width: 300px;
            padding: 40px;
            background-color: rgba(242, 242, 242, 0.226);
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(13, 232, 248, 0.2);
        }

        .register-form::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: inherit;
            filter: blur(10px);
            z-index: -1;
            border-radius: 10px;
        }

        .register-form input[type="text"],
        .register-form input[type="password"] {
            width: 100%;
            padding: 10px;
            border: none;
            border-bottom: 2px solid #aaa;
            outline: none;
            transition: border-bottom-color 0.3s;
            background-color: rgba(255, 255, 255, 0.5);
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        .register-form input[type="text"]:focus,
        .register-form input[type="password"]:focus {
            border-bottom-color: #33adff;
        }

        .register-form h2 {
            text-align: center;
            color: #100f0f;
            margin-bottom: 30px;
        }

        .register-form .form-group {
            margin-bottom: 20px;
        }

        .register-form label {
            display: block;
            margin-bottom: 5px;
            color: #020101;
        }

        .register-form button[type="submit"] {
            display: block;
            width: 50;
            padding: 10px;
            background-color: #191919;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
            margin: 0 auto;
        }

        .register-form button[type="submit"]:hover {
            background-color: #1a48ff;
        }
    </style>
</head>
<body>
    <header>
        <h1 style="color:white; text-align: center;">Register for VR Tourism</h1>
        <nav>
            <a href="/" style="position: absolute; left: 50px; top: 60px; font-size: 1.5em; color:blue;">Home</a>
            <a href="/login" style="position: absolute; right: 50px; top: 60px; font-size: 1.5em; color:blue;">Login</a>
        </nav>
    </header>
    <div class="register-container">
        <form method="POST" action="/registrations" class="register-form">
            <h2>Register</h2>
            <div class="form-group">
                <label for="email">E-mail</label>
                <input type="text" id="email" name="user[email]" required>
            </div>
            <div class="form-group">
                <label for="username">Name</label>
                <input type="text" id="username" name="user[name]" required>
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="user[password]" required>
            </div>
            <button type="submit">Register</button>
        </form>
    </div>
</body>
</html>
