<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Form</title>
    <style>
           
body {
    font-family: Arial, sans-serif;
    background-image: url('<%= asset_path('549551.jpg') %>');
    background-size: cover;
    margin: 0;
    padding: 0;
    height: 100vh;
}

.login-container {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100%;
}

.login-form {
    position: relative; 
    width: 300px;
    padding: 40px;
    background-color: rgba(242, 242, 242, 0.226);
    border-radius: 10px;
    box-shadow: 0 0 20px rgba(13, 232, 248, 0.2);
}


.login-form::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: inherit; /* Inherit the background */
    border-radius: inherit; /* Inherit the border radius */
    filter: blur(10px); /* Apply blur effect */
    z-index: -1; /* Ensure it's behind the form */
}

h2 {
    text-align: center;
    color: #131212;
    margin-bottom: 30px;
}

.form-group {
    margin-bottom: 20px;
}

label {
    display: block;
    margin-bottom: 5px;
    color: #000000;
}

input[type="text"],
input[type="password"] {
    width: 100%;
    padding: 10px;
    border: none;
    border-bottom: 2px solid #1a48ff;
    outline: none;
    transition: border-bottom-color 0.3s;
}

input[type="text"]:focus,
input[type="password"]:focus {
    border-bottom-color: #33adff;
}

.custom-button {
            display: block;
            background-color: #161515;
            color: #fff; /* Button text color */
            border: none;
            border-radius: 5px;
            cursor: pointer;
            padding: 10px;
            transition: background-color 0.3s;
            width: 45%;
            margin: 0 auto;
        }

.custom-button:hover {
            background-color: #005ccb;
        }


.register-link {
    display: flex;
    align-items: center;
    color:#161515;
}

.register-link p {
    margin-right: 10px; /* Adjust spacing as needed */
}



    </style>
</head>
<body bgcolor="Black" style="color: white;">
    <div class="login-container">
        <%= form_with(url: login_path, scope: :session, local: true, class: "login-form") do |form| %>
            <h2>Login</h2>
            <div class="form-group">
                <%= form.label :email, "Email" %>
                <%= form.text_field :email, id: "email", required: true %>
            </div>
            <div class="form-group">
                <%= form.label :password, "Password" %>
                <%= form.password_field :password, id: "password", required: true %>
            </div>
            
            <%= form.submit "Login", class: "custom-button" %>
            <br><br>
            <div class="register-link">
                <p>Create an account if you haven't</p>
                <a href="<%= register_path %>" style='color:violet'>Register</a>
            </div>
        <% end %>
    </div>
    <a href="/" style="position: fixed; bottom: 20px; left: 50%; transform: translateX(-50%); font-size: 1.5em; color: white;">Back to Home</a>
</body>
</html>
